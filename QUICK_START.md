# Chess AI - Quick Start Guide

## ✅ Backend is Running!
Your FastAPI backend is running on http://localhost:8000

## 🚀 Start the Frontend

### Option 1: Automatic (Recommended)
Run this command:
```bash
cd web
npm install
npm run dev
```

### Option 2: Step by Step
1. Open a NEW terminal
2. Navigate to web folder:
   ```
   cd C:\Users\narut\OneDrive\Documents\GitHub\Chess-AI\web
   ```
3. Install dependencies (first time only):
   ```
   npm install
   ```
4. Start the dev server:
   ```
   npm run dev
   ```

The web app will open at **http://localhost:3000**

## 🎮 How to Play

1. Open http://localhost:3000 in your browser
2. Drag and drop white pieces to move
3. AI (black) will respond automatically
4. View analysis on the right side
5. Adjust difficulty with the depth selector
6. Click "New Game" to restart

## 📋 What's Running

- **Backend (Python)**: http://localhost:8000 - Chess engine API
- **Frontend (React)**: http://localhost:3000 - Web interface

Both need to be running at the same time!

## 🛑 To Stop

- Backend: Press CTRL+C in the backend terminal
- Frontend: Press CTRL+C in the frontend terminal
